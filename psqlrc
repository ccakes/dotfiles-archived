\set QUIET 1
\pset null '¤'

\set PROMPT1 '%[%033[1m%][pg:%n@%m] %~%x %[%033[0m%]'
-- SELECT * FROM<enter>. %R shows what type of input it expects.
\set PROMPT2 '... > '

\timing

\x auto
\set VERBOSITY verbose
\set HISTCONTROL ignoredups
\set COMP_KEYWORD_CASE upper
\unset QUIET
